{
  "name": "opencode-webui",
  "version": "0.3.15",
  "private": true,
  "type": "module",
  "packageManager": "pnpm@9.15.0",
  "workspaces": [
    "shared",
    "backend",
    "frontend"
  ],
  "scripts": {
    "predev": "node scripts/predev.js",
    "opencode:install": "node scripts/install-opencode.js",
    "agent-browser:install": "node scripts/install-agent-browser.js",
    "agent-browser:update": "node scripts/install-agent-browser.js --force",
    "dev": "concurrently --kill-others \"pnpm:dev:backend\" \"pnpm:dev:frontend\"",
    "dev:backend": "bun --watch backend/src/index.ts",
    "dev:frontend": "pnpm --filter frontend dev",
    "build": "pnpm run build:backend && pnpm run build:frontend",
    "build:backend": "pnpm --filter backend build",
    "build:frontend": "pnpm --filter frontend build",
    "test": "pnpm run test:backend",
    "test:backend": "pnpm --filter backend test",
    "generate:openapi": "bun scripts/generate-openapi.ts",
    "docker:build": "docker-compose build",
    "docker:up": "docker-compose up -d",
    "docker:down": "docker-compose down -v",
    "docker:logs": "docker-compose logs -f",
    "docker:restart": "docker-compose restart"
  },
  "devDependencies": {
    "concurrently": "^9.1.0"
  },
  "dependencies": {
    "class-variance-authority": "^0.7.1",
    "dotenv": "^17.2.3"
  }
}
